{
  "name": "daily-planner",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . daily-planner --platform=win32 --arch=x64 --icon=icon.png --out=dist --overwrite"
  },
  "dependencies": {
    "electron": "^28.0.0"
  }
}